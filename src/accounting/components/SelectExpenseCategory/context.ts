import { createContext } from 'solid-js';

import type { SelectExpenseCateogryContextProps } from './types';

export const SelectExpenseCategoryContext = createContext<SelectExpenseCateogryContextProps>({});
